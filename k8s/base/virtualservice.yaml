apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: events-api
  labels:
    app.kubernetes.io/name: events-api
spec:
  hosts:
    - events-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: events-api
            port:
              number: 80
          weight: 100
